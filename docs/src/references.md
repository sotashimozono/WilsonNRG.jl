# References

Published results that `WilsonNRG.jl` reproduces. Every DOI was resolved and verified
via [`doiget`](https://github.com/sotashimozono/doiget); open-access full texts are kept
locally under `docs/refs/` (git-ignored).

```@bibliography
*
```
